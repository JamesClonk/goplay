package main

type Config struct {
	ForceCompile    bool
	CompleteBuild   bool
	GoplayDirectory string
}

// Read configuration
func ReadConfigurationFile(filename string) (config *Config) {
	return config
}
